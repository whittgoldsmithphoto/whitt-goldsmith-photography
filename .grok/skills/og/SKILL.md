## Brand-asset pass:

The marker is `/workspace/.grok/og-pending` and is stale after 10 minutes.
Run `node scripts/brand-check.mjs --placeholder-ok` when checking a utility app.

Never wait_tasks for this brand task and never get_task_output for this brand task.
Do not wait_tasks before the final verify and do not get_task_output before the final verify.
